# Copy to .env and fill in real values for local dev. Never commit .env —
# it's already gitignored. In production (Render), set these as real
# environment variables instead; .env is not used there.

GEMINI_API_KEY=
